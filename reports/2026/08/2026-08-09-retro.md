# Jamasp Retro — week ending 2026-08-09

*First real scorecard. 35 predictions resolved this week (scored 3–9 Aug):
**25 hit, 8 miss, 2 void** — 75.8% on n=33 determinate. Open book going
into CPI week: 7 predictions (`951d8286`, `3d8416e4` closes tonight,
`ec71ce01`, `3e704df1`, `5fef3d21`, `985e77d0`, `772323d6`).*

## Scorecard

**By claim type:**

| Type | Record | Avg stated conf | Read |
|---|---|---|---|
| Diplomacy — structural skepticism ("no confirmation / no joint text / regime persists") | **6/6** | 0.76 | The week's engine. `96448cc2` `369d0a65` `90ee5bdd` `50db7544` `856cf923` `b8d67af8` — every one hit, most by a wide margin. |
| Diplomacy — announcement timing ("deal announced by \<date\>") | **0/3** | 0.55 | `7fcb559d` `fb90abae` `df9a603b` — the same deadline slid Tue→Wed→Fri and never landed. Worse: confidence *rose* with each miss (0.50 → 0.55 → 0.60), chasing media momentum. |
| Rates / price path (floors, caps, gaps, fades) | **12/16** | 0.63 | Right-reason core: the Monday fade family (`dc38b83f` `78a3c1ab` `1438e2a6`) nailed mechanism and timing (13:20Z rates-session break, pre-ISM). All 4 misses are one family: gap-size overreach (`2b0d5f3c` `08d74cee`, capped by Trump's pre-reopen "deal done") and the two range caps (below). |
| Kinetic / maritime | **5/6** | 0.68 | Pattern-persistence claims all hit; the one miss (`aba1ae09`) wrote "no vessel damage" while the UKMTO feed was dark — ADNOC's operator confirmation (1 dead, 20 injured) falsified it. |
| Basis / microstructure | **2/2** | 0.68 | `cb2a8977` `12360fdd` — the 31 Jul ~$75 LBMA–COMEX gap fully resolved as stale-fix timing + Dec-26 carry, not a physical dislocation. No desk action needed. |

**Quality adjustments the raw count hides:** two hits were wrong-reason
(`6f772055` — gap held on residual floor, not the claimed war bid;
`7bca8d8f` — level reached via NFP repricing, not the claimed strike) and
are discounted in calibration. Per-event grouping (last retro's warning):
the ~10 Monday-reopen predictions collapse to ~6 distinct questions; the
only grouped miss there was gap size. The week's headline number is
flattered by the reopen cluster but the per-event record still supports it.

**Where I'm reliably right:** betting *against* announced diplomacy until
both capitals co-confirm; mechanism calls tied to a session clock (the
rates-desk fade); maritime pattern persistence.

**Where I overweight noise:** deadline stories sourced to one side's
officials; range caps justified by a single-driver narrative; gap-size
precision at event reopens.

## Calibration notes

Stated vs observed (determinate n=33):

| Stated | n | Observed |
|---|---|---|
| ≥0.80 | 4 | 100% |
| 0.70–0.79 | 11 | 91% |
| 0.60–0.69 | 11 | 64% |
| 0.50–0.59 | 5 | 60% |
| <0.50 | 2 | 50% |

The 0.5–0.69 range is honest. Above 0.70 I am **underconfident** — and the
composition says why: that's where the structural-skepticism claims live.
The asymmetry is one bias with two faces, and I name it **coverage-equals-
probability**: as coverage volume on the Hormuz announcement grew, I raised
confidence in the *event* (0/3), while leaving the mirrored *skepticism*
claims — which the same evidence supported — at 0.7–0.85 when they were
running 100%. Volume of reporting measures attention, not likelihood.

## Playbook changes

Playbook rewritten 5 → 11 heuristics. All four queued lessons consumed.

- **Confirmed (was under test):** #3 — non-response identifies the marginal
  price-setter. The Monday fade family hit right-reason on exactly that
  mechanism. **Revised** with a sharper edge: the signal names *who* sets
  the price, not *which way* — misreading it as "range-bound" produced both
  cap misses.
- **Promoted from lessons inbox:**
  - Range caps must state the direction of the controlling desk's live
    impulse (`f7d39052`, `394dfea7` — both caps broke upward, no war
    headline, rates/debasement channel).
  - "Only X can cause Y" claims must enumerate and reject the third channel
    or drop "only" (`394dfea7` — stagflation-data + debasement bid broke R1).
  - One-side-sourced diplomatic deadlines: cap ≤0.5, never raise on
    coverage volume (0/3 this week).
- **Promoted from prediction notes:**
  - No-incident claims need a live incident feed (`aba1ae09` — UKMTO dark,
    operator confirmation falsified the claim).
  - Separate trigger from target in compound claims; wrong-reason hits get
    discounted (`7bca8d8f`, `6f772055`).
  - The skeptic's edge is priceable: structural-negative diplomacy claims
    backed by on-record positions deserve 0.8+, not 0.65–0.75 (6/6 at 0.76).
- **Pruned:** nothing — no heuristic is 4+ weeks unused (book is 2 weeks old).
- `state/lessons-inbox.md` consumed and emptied.

## Feedback

Searched `reports/` and `state/lessons-inbox.md` for `feedback:` entries —
**none from Saman this week.** Nothing to adopt or decline. Standing item
re-raised in the Telegram summary: the gcaptain ingest proposal from the
2 Aug retro is still not in `config/sources.yaml`, so the maritime blind
spot is still covered manually by playbook rule 4. Awaiting a yes/no.

## Follow-ups from last retro

1. Per-event + per-prediction scoring — done (above); first calibration
   curve produced.
2. Heuristic 3 verified against the Monday tape — confirmed, promoted with
   revision.
3. gcaptain ingest proposal — **not implemented**; re-raised to Saman.
4. `cb2a8977` — resolved as data artifact ($28 basis on 4 Aug); the
   physical-dislocation worry is closed.

## For next retro

1. CPI week book: `5fef3d21`, `772323d6`, `985e77d0`, `3d8416e4` resolve
   Mon–Tue; score against the branch-point framing in stance, and check
   whether the "no fresh directional predictions pre-CPI" discipline held.
2. Test the new deadline rule: any announcement-timing claim this week must
   carry ≤0.5 unless anchored to a scheduled event.
3. Watch the 0.7+ bucket: if structural-skepticism claims keep printing
   ~100%, start writing them at 0.8+ and verify calibration follows.
4. Calendar feed is dark again (0 events/14d, regressed after the 2 Aug
   repair) — maintenance item, affects event-anchored predictions.

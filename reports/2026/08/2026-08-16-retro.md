# Jamasp Retro — week ending 2026-08-16

*13 predictions resolved this week (scored 10–16 Aug): **10 hit, 2 miss,
1 void** — 83.3% on n=12 determinate, up from 75.8% last week. Open book:
11 predictions, concentrated on next week's gates (FOMC minutes Wed,
corridor close-outs Thu–Fri, Bessent package by Fri).*

## Scorecard

**By claim type:**

| Type | Record | Avg stated conf | Read |
|---|---|---|---|
| Diplomacy — structural skepticism ("no co-confirmed text") | **2/2** | 0.70 | `985e77d0` `3353195f` — reparations wall + seven-conditions gate held both windows. Family streak now **8/8 over two weeks**, still priced at 0.70. |
| Kinetic/maritime — no-confirmation claims | **4/4** | 0.61 | `1d282df6` (Sirik never verified), `0aaec9a2` (nameless Gulf strike = echo), `8f491cb0`/`ee2638be` (Jizan claim hollow). The verification-gate discipline is working exactly as designed. |
| Kinetic/maritime — pattern extrapolation | **0/1** | 0.75 | `71096269` — "ADNOC vessels named within 48h like every prior hit." Three operator-confirmed hits still nameless 60h+ on. The naming *pipeline* broke (UAE info-control or registry channel gone quiet) while the surface pattern looked intact. |
| Rates/price path (levels, order-of-events) | **4/4** | 0.63 | `5fef3d21` (4300 floor to CPI), `772323d6` (no 200DMA tag pre-CPI), `e3a35539` (200DMA tag by Fri — to the tick, 4501.8 at 13 Aug 00:35Z), `3e704df1` (200DMA before 50DMA settle). The two-sided 200DMA framing — "not before CPI, yes after" — was the week's cleanest work. |
| Rates/data — pattern extrapolation | **0/1** | 0.55 | `9a757c1a` — "Brent +44% YTD reaches PPI." Printed flat. Demand destruction reached pipeline prices before the war leg did; second failed read of oil-passthrough in two days (benign CPI 12 Aug). |
| Void | 1 | — | `3d8416e4` — announcement-shape conditional never armed; correctly VOIDed per its own terms. Claim hygiene held. |

**The week in one line:** every skeptic-shaped claim hit (10/10 at avg
0.64); both affirmative "the pattern continues" claims missed (0/2 at avg
0.65, including the week's highest-confidence claim).

**Process failure the count hides:** `e3a35539` HIT, but the tag printed
13 Aug 00:35Z in the Asia overnight and the 13 Aug brief declared the level
"untagged" ~3h *after* the print — then two more runs carried "miss-track"
from narrative memory while the falsifying print sat in the DB. Right call,
wrong bookkeeping, three runs of wrong stance built on it.

**Where I'm reliably right:** structural-negative claims wherever the
process is on record (diplomacy, verification gates); event-framed level
claims with explicit windows; claim hygiene (VOID terms, scoping to
available feeds per playbook #9 — `1d282df6` was written correctly scoped
with UKMTO dark).

**Where I overweight noise:** observed regularities without a named
transmission mechanism. Both misses assumed a pipeline (price passthrough,
naming-in-48h) would keep delivering without asking what could interrupt
it. Last retro filed "maritime pattern persistence" as a strength; this
week shows the strength was always the *verification gate*, not the
extrapolation.

## Calibration notes

Stated vs observed (determinate n=12):

| Stated | n | Observed |
|---|---|---|
| 0.70–0.79 | 4 | 75% |
| 0.60–0.69 | 6 | 100% |
| 0.50–0.59 | 2 | 50% |

The 0.60s ran perfect — that bucket is where the skeptic claims live, so
it's the same underconfidence as last week wearing a new bucket. The only
0.75 of the week was the miss.

**The concrete bias, named: confidence follows vividness, not process
strength.** Last retro's follow-up #3 said explicitly: start writing
structural-skepticism claims at 0.8+. I didn't — `985e77d0`, `3353195f`,
`8f491cb0` were all created at 0.70 *after* that instruction, and the
scoring notes flagged playbook-#11 non-compliance twice in real time.
Meanwhile the one claim that told a vivid story ("names always surface in
48h") got 0.75, my highest of the week, and missed. The rule existed; the
discipline to apply it at creation didn't. Fixed in this rewrite by making
#11 a mechanical floor rather than advice.

## Playbook changes

Playbook rewritten 11 → 16 heuristics. All four queued lessons consumed.

- **Revised:** #11 (skeptic's edge) hardened from advice to a mechanical
  floor — structural-negative claims backed by on-record positions *start*
  at 0.8; going lower requires naming the specific gate that justifies it.
  Evidence: 8/8 across two weeks while stated confidence sat at 0.55–0.76;
  non-compliance flagged twice in this week's own scoring notes.
- **Promoted from this week's misses:**
  - Pattern-extrapolation claims must name the live transmission mechanism
    or cap at 0.6 (`9a757c1a`, `71096269` — the week's only two misses,
    same shape).
  - Operator confirmation stands alone: namelessness downgrades detail,
    not the event. Two-sided rule merging the `0aaec9a2` hit (nameless +
    operatorless = echo) with the `71096269` miss (requiring names on an
    operator-confirmed hit = degraded).
- **Promoted from lessons inbox:**
  - Level-claim status comes from a DB max/min query over the claim
    window, never narrative memory (`e3a35539` — three runs wrong after
    an Asia-overnight tag).
  - Data-event deepdives schedule ≥40 min post-print; a run that finds
    its inputs missing reschedules via `wakeup add` and still commits —
    silent exit-0 runs are invisible to alerting (12 Aug CPI deepdive,
    desk read ~3h late).
  - Extract freshness: check `extract_cache.fetched_at` before trusting
    any index/section extract; date agency copy from its own dateline
    (16 Aug — stale 13 Aug AJ snapshot + July-9 Reuters "standstill" wire
    both caught in one run).
- **Pruned:** nothing — no heuristic is 4+ weeks unused (book is 3 weeks
  old). First prune eligibility ~30 Aug.
- `state/lessons-inbox.md` consumed and emptied.

## Feedback

Searched `reports/` and `state/lessons-inbox.md` for `feedback:` entries —
**none from Saman this week.** Nothing to adopt or decline. Standing item
re-raised again in the Telegram summary: the gcaptain ingest proposal
(from the 2 Aug retro) is still not in `config/sources.yaml`; the maritime
blind spot is still covered manually by playbook #4. Awaiting a yes/no.

## Follow-ups from last retro

1. CPI-week book — resolved as framed: `5fef3d21` `772323d6` `985e77d0`
   hit, `3d8416e4` correctly voided. The "no fresh directional predictions
   pre-CPI" discipline held.
2. Deadline-rule test — no two-party announcement-timing claims were
   written this week (rule #8 territory stayed clean). Note: `cd49c313`
   (Bessent package by Fri, 0.6) is *actor-controlled* — the announcing
   party owns the deadline — so it sits outside #8's one-side-sourced
   scope. It closes Friday and serves as the live test of that boundary.
3. 0.7+ bucket instruction — **not adopted at creation; the week's main
   calibration failure.** Now mechanical (revised #11).
4. Calendar feed — **still broken** (0 events/14d). Re-raised as a dev
   task; affects event-anchored claim windows.

## For next retro

1. Heavy close-out week: `540428e8` (minutes, Wed), `a64b23c1`/`da6f26d5`/
   `cf62fcf6`/`9557c060`/`599d9586` (corridor + Brent, Thu–Fri),
   `cd49c313` (Fri), `b6794dbb` (Sun). Score the revised-#11 floor: were
   new structural-negative claims actually written at 0.8+?
2. Test new heuristic #12: any pattern-extrapolation claim this week must
   name its transmission mechanism in the claim text.
3. ADNOC naming: if vessel names eventually surface, backfill *when* and
   through which channel — it calibrates how degraded the naming channel
   really is (info-control vs registry lag).
4. Calendar feed repair — carry until fixed.

# Jamasp Retro — week ending 2026-08-23

*15 predictions resolved this week (scored 17–23 Aug): **11 hit, 3 miss,
1 unclear** — 78.6% on n=14 determinate (last week 83.3% on n=12, prior
75.8%). Open book: 17 predictions, front-loaded on this coming week's
gates (Bessent presser Mon, core PCE Wed 26 Aug 12:30Z, Warsh + benchmark
revision Fri 28 Aug). `b6794dbb` is unscored by design — see below.*

## Scorecard

**By claim type:**

| Type | Record | Avg stated conf | Read |
|---|---|---|---|
| Structural-negative (diplomacy + kinetic verification gates + behavior persistence) | **5/5** | 0.70 | `213a5792` (no US confirmation of Lincoln damage), `4cf94ab6` (no co-confirmed extension text), `d2ea5d99` (Amara seizure never operator/flag-confirmed), `da6f26d5` (ADNOC condemn-and-continue held), `cf62fcf6` (US response stayed economic). Family now **13/13 across 3–23 Aug**, still priced 0.55–0.80. |
| Affirmative event, mechanism named | **1/1** | 0.80 | `a64b23c1` — Houthi campaign-continuation claim hit via the 19 Aug Saree statement. The compliant shape rule 12 asks for: declared campaign + observed weekly cadence = live transmission mechanism, priced at 0.8, paid. |
| Rates / central banks | **2/2** | 0.68 | `31c34147` (PBOC LPR hold), `540428e8` (minutes don't revive the Sept hike). Clean week. |
| FX / dollar | **1/1** | 0.60 | `0deccd97` — yen debasement pillar held (no USDJPY close <157; closed 158.94). |
| Oil / flows | **1/1** | 0.65 | `9557c060` — Brent thin-buffer floor ($80) held with $9–14 of room. |
| Price path — levels | **1/3** | 0.60 | Hit: `3f03ae58` (200DMA breakout hold, with-trend). Misses: `a2e9d1a8` (4450 cap, broke 11 days early), `7e803666` (4510 cap, broke next session on the buyback catalyst). Both misses were **against-trend caps**; the one hit was the **with-trend hold**. |
| Announcement timing | **0/1** | 0.60 | `cd49c313` — Bessent package slipped Fri→Mon presser. This was rule 8's named live test of the actor-controlled carve-out; the carve-out failed. |
| Instrument failure | 1 unclear | 0.60 | `599d9586` — UKMTO weekly traffic figure never relayed by press (the claim's own unclear branch), and the AIS-share metric it rested on broke mid-window: 80% of Hormuz transits now deliberately dark (Kpler, 23 Aug), DoE puts real flow ~2x tracked. Metric retired. |

**The week in one line:** everything with a named mechanism or an on-record
structural fact hit (11/11 at avg 0.69); everything that fought the trend or
trusted a calendar nobody controls missed (0/3 at 0.60).

**Where I'm reliably right:** structural negatives (three straight perfect
weeks — this is the desk's edge, and it is *systematically underpriced*);
rates/CB event claims; with-trend level claims carrying an explicit flow
mechanism.

**Where I overweight noise:** against-trend caps in a confirmed trend regime
(twice this week, both compliant with heuristic 6's 0.6 cap — the damage was
contained but the claims should not have been written); announcement
timing without a scheduled anchor (fourth miss of this shape in three weeks).

**`b6794dbb` (no Saudi-confirmed infra damage through tonight 23:59Z):** the
CLI marks it matured (horizon_days rounds to 03:34Z) but the claim's own
window runs to 23:59Z tonight. A DB sweep this run found only the Yemeni-side
18 Aug drone claim (`mehr_en`), no Saudi/Aramco confirmation — on-track HIT,
but a no-event claim is determinate only at window close. Scoring it 8h early
would be the same class of error rule 5 exists to prevent. The overnight scan
carries it, per stance.

## Calibration notes

Stated avg 0.664 vs observed 0.786 on n=14 — underconfident overall for the
third consecutive week. But the bucket view shows the real shape:

| Stated | Record | Observed |
|---|---|---|
| 0.60 | 2/5 | **40%** |
| 0.65 | 2/2 | 100% |
| 0.70 | 6/6 | 100% |
| 0.80 | 1/1 | 100% |

**The concrete bias: 0.6 is being used as a hedge label, not a probability.**
The confidence distribution is compressed into [0.6, 0.7] from both sides.
Claims that belong at ≤0.5 (announcement timing) or that shouldn't exist
(against-trend caps) get parked at 0.6; claims that have earned 0.8+ (the
13/13 structural-negative family) get squeezed to 0.7 — and this week
identified the mechanical cause of the top-side squeeze: the brief skill
hard-caps `--confidence` at 0.7 (`.claude/skills/brief/SKILL.md:82`), so
every brief-created structural negative is *forced* non-compliant with
playbook rule 11's 0.8 floor. Three scoring notes said "capped by brief 0.7
ceiling" this week alone (`4cf94ab6`, `d9e50362`, `fe35b77d`), while
scan/deepdive-created siblings (`a64b23c1`, `bb86ccc9`) sat correctly at
0.8. Filed as **todo-007** — the ceiling made sense as protection against
breathless news reads; it is now costing calibration exactly where the
process is strongest.

## Playbook changes

Rewritten in full; 16 → 18 heuristics, nothing pruned (nothing is 4+ weeks
unused — oldest evidence is 30 Jul).

- **#4 revised** (maritime press): while todo-005 is open, any assertion
  about what the maritime press does/doesn't carry must sweep gcaptain
  `?s=` search, not just ingested items — evidence: the feed missed the
  campaign-defining Wafa article 19 Aug while ingesting 13 other gcaptain
  items that day.
- **#6 revised** (range caps): trend-regime extension — in a confirmed
  trend, an against-trend cap needs a fresh counter-mechanism named or
  isn't written — evidence: `a2e9d1a8`/`7e803666` missed at compliant 0.6
  while with-trend `3f03ae58` hit.
- **#8 revised** (announcement timing): the actor-controlled carve-out is
  removed after failing its named live test (`cd49c313`); ALL
  announcement-timing claims ≤0.5 unless anchored to a published, scheduled
  event.
- **#11 revised** (structural-negative floor): 13/13 evidence updated; notes
  the brief-ceiling conflict and, until todo-007 resolves, requires naming
  the compliant floor in the claim text so calibration can correct.
- **#14 generalized** (status from the DB): now covers price levels, event
  trajectories, AND infrastructure health — run the check
  (`jamasp predictions due --open`, a keyword query, or the component
  itself) in the same run, never assert from narrative memory — evidence:
  `a64b23c1` trajectory inverted same-day; the calendar "outage" phantom
  carried by three retros and disproven both times someone ran
  `jamasp calendar`.
- **#15, #16 rewritten to point at tooling** (landed 17 Aug): `jamasp run`
  now records empty runs and notifies the desk; `extract` now prints
  `fetched_at`/age and re-fetches stale cache. The surviving manual halves
  (≥40-min post-print scheduling; dateline discipline) stay.
- **#17 NEW** (scheduled-date anchors): any wakeup/stance anchor on a
  data/event date cites a primary-calendar verification ≤7 days old;
  Saturday brief owns the weekly sweep — evidence: three hand-built date
  errors in ten days (FOMC minutes, Warsh, PCE).
- **#18 NEW** (falsifier observability + instrument validity): falsifiers
  must be scoreable from observable-at-will state or committed-schedule
  releases, and adversary-controllable telemetry (AIS) gets an
  is-the-instrument-valid check — evidence: `599d9586`.

## Human feedback (Saman, 17 Aug)

1. **"Khamenei's death was not a coverage blind spot" — adopted.** The
   2 Aug retro's blind-spot list is corrected on the record: the Jordan
   missile attack and Damietta FSRU strike stand as genuine feed failures
   (both moved Brent >8% intraday inside the archive window); the Khamenei
   item is struck — a long-past event was never findable in a three-day
   archive and shouldn't have been cited as source-gap evidence.
2. **Approved: gcaptain, an Iranian-press feed, an incident feed — adopted.**
   gcaptain is already wired (its reliability gap is todo-005). The
   remaining two need per-feed fetch/extract verification from the host
   before wiring (Press TV/Tasnim/IRNA and UKMTO/CENTCOM all 403 on extract
   from this host; Mehr works and is already ingesting). Filed as
   **todo-008** with the verification matrix, so this stops being re-raised
   in prose. When both land, #4's manual half and #9's scoping narrow —
   noted in both heuristics.

## Verified-this-run (rule 14 discipline)

- `jamasp calendar` — **alive**, 18 events, coverage through 29 Aug: core
  PCE confirmed Wed 26 Aug 12:30Z and Warsh Fri 28 Aug (the 22 Aug
  two-extract verification now has a feed row behind it). The stance's
  "calendar feed dead past 21 Aug" was true only until the thisweek-only
  window rolled over Sunday — todo-001 (horizon) remains the real item; the
  stance's sourcing-health line is corrected this run.
- **Discrepancy flagged for Monday:** wakeup #35 fires 16:00Z for the
  Bessent presser; the calendar feed carries "Treasury Sec Bessent Speaks"
  at **18:00Z** (Medium). Possibly two engagements. Early is recoverable
  (an empty run reschedules per #15); noted in stance so the Monday runs
  verify rather than trust either time.
- `jamasp predictions due --open` — working as specced (per-claim window
  high/low annotations present); heuristic 14's tooling confirmed live.

## Todos filed/updated this run

- **007** (new): brief skill's 0.7 confidence ceiling vs playbook rule-11's
  0.8 floor — decision needed on the ceiling's shape.
- **008** (new): wire the two approved sources (Iranian press, incident
  feed) with per-feed host verification first.
- 001, 005: referenced, still open, nothing new to append.

# Jamasp Retro — week ending 2026-09-06

*This retro covers two weeks. The 30 Aug retro never ran — `agent_runs`
id 311, exit 1 after 2s, the OAuth refresh-token lapse (todo-007 for the
alert side, todo-013 for why nobody noticed the missing output). Lessons
inbox and playbook were last consumed/rewritten 23 Aug.*

*23 predictions scored 24 Aug–5 Sep: **15 hit, 8 miss** — 65.2% on n=23
(23 Aug retro: 78.6% on n=14; before that 83.3%, 75.8%). One matured claim,
`fe35b77d`, is unscored by design — see below. Open book: 27, front-loaded
on tonight's reopen (3), 7 Sep (10), and the PPI/ECB/CPI gate 10–11 Sep (6).*

## Scorecard

**By claim type:**

| Type | Record | Avg stated | Read |
|---|---|---|---|
| Structural-negative (no co-confirmed text / no designation / regime persists) | **9/9** | 0.76 | `b6794dbb` `d8fda2a3` `f230eca0` `73281b56` `ac586003` `adc2a252` `d9e50362` `f73172f8` `2d9fac65`. Family now **22/22 across 3 Aug–1 Sep**. Five of the nine were brief-created and ceiling-capped at 0.7 (todo-007 still open). |
| Affirmative event, mechanism named | **1/2** | 0.70 | Hit `bb86ccc9` (Houthi campaign continuation, 0.8 — declared campaign + weekly cadence, scored 24 Aug on the AMZAN strike via gcaptain + Maritime Executive). Miss `8babc60f` (Iran *frames* enforcement as a response to sanctions, 0.6) — enforcement continued all window, the framing never came. A claim about how an actor will phrase something is announcement-shaped. |
| Rates / Fed pricing | **1/5** | 0.65 | Hit `1a08ec53` (Warsh gives no explicit Sept leaning). Misses: `98bbfd33` (odds <50% thru payrolls — Warsh tone alone took CME to 56%), `021357a6` (odds not ≥65% pre-NFP — Barr + Warsh follow-through printed ~70% on 1 Sep), `16085af9` (odds ≥60% at the print — Waller's HOLD lean cut it to 50% on 3 Sep), `951d8286` (ISM prices-paid <71.1 — printed 71.1, unchanged; mechanism written 3 Aug on Brent <84 and a US–Iran pause, overtaken by the Hormuz war over a 29-day horizon). |
| Price path — gold levels | **2/3** | 0.65 | Hits: `9b2f3adf` (4510 floor into PCE, right-reason on buyback flow), `2ef33025` (4300 floor through NFP, right-reason: the dip was bought). Miss: `031e3470` (4457 cap pre-NFP) — the against-trend cap again. **Gold caps are now 0/5 since 5 Aug; gold floors/holds 4/4.** |
| Dollar / oil levels with a flow mechanism | **2/2** | 0.60 | `b0c2bfeb` (DXY no close ≥100 through core PCE; max 99.69 on Warsh day), `6f8b0433` (Brent no settle ≥95; peak ~94 intraday). Both against-trend caps *outside gold* with a named supply/flow mechanism — they held. |
| FX pillar | **0/1** | 0.65 | `b6450c7a` (USDJPY no close <157 through 4 Sep) — closed ~155.8. Priced only the MoF-intervention break channel; BoJ hawks + Japan data + US dovish repricing were the actual path. Falsifier 1.4% from spot over ten days in a pair running >1% daily ranges. |
| Announcement timing | **0/1** | 0.50 | `55412a56` (Bessent "major" designation by 30 Aug) — landed as a *proposed rulemaking*, not a designation. Written at the rule-8 cap of 0.5; 0/4 lifetime for the shape, the cap is priced right. |

**The fortnight in one line:** everything with an on-record structural fact
or a named flow mechanism hit (13/14 at avg 0.70); every claim that put a
threshold on Fed pricing across an FOMC-speaker window missed (0/3, broken
by *tone*, one in each direction); and the gold cap failed for the fifth
straight time.

**Where I'm reliably right:** structural negatives (22/22 — the desk's
edge, still under-priced by the brief ceiling); with-trend gold floors;
non-gold level claims carrying a supply/flow mechanism.

**Where I overweight noise:**

1. **Fed-pricing thresholds across speaker windows.** `98bbfd33`,
   `021357a6`, `16085af9`: each named data as the only mover and treated
   tone-without-guidance as the tail. It was the base case — three
   repricings of 6–20 points in eight days, none on data.
2. **Against-trend gold caps.** `031e3470` was rule-3's misread again:
   "three sessions of war escalation failed to lift gold" was read as
   "rates desk has the wheel, impulse down" and became a cap; the rates
   desk's own impulse flipped on Waller the day the cap was tested.
3. **Thin falsifiers on FX pillars.** `b6450c7a` — and open `a1fc647f`
   (4425 floor, 51 points from spot on a 113 ATR, one-day window, 0.65)
   has the same shape. Flagged for calibration rather than rewritten.
4. **Stale-mechanism long horizons.** `951d8286` carried a 3 Aug premise
   for 29 days and missed by zero margin on 1 Sep. Nothing re-checked it
   after the war restarted.

**`fe35b77d` (no suspension/reduction of US escort operations through 6 Sep
23:59Z):** the CLI marks it matured (horizon arithmetic lands at 03:32Z),
the claim's window closes tonight. DB sweep this run: no suspension, no
reduction — the opposite (2 Sep CENTCOM escorted a record 18 mb through
Hormuz, Gulf News/CNN; 3 Sep Trump's three-pronged strategy leads with
"escort ships"). On-track HIT; the 7 Sep brief scores it at window close,
same discipline as `b6794dbb` on 23 Aug. Noted in stance.

## Calibration notes

Stated avg 0.685 vs observed 0.652 on n=23 — the first **over**confident
period after three underconfident weeks. The bucket view:

| Stated | Record | Observed |
|---|---|---|
| 0.50 | 0/1 | 0% |
| 0.60 | 2/5 | **40%** |
| 0.65 | 3/5 | 60% |
| 0.70 | 5/7 | 71% |
| 0.80 | 3/3 | 100% |
| 0.85 | 2/2 | 100% |

For the first time the buckets are monotone — the ordering is right. But
0.60 sits at 40% for the second retro running (2/5 both times): it is still
the hedge label. Its two hits (`b0c2bfeb`, `6f8b0433`) were level claims
with a named mechanism; its three misses were an odds threshold, a framing
claim and another odds threshold. Those belong at 0.5 or don't get written.

**The concrete bias:** *market-pricing claims assume only data moves
pricing.* Every odds claim this fortnight enumerated scheduled data and a
de-risking fade, and none listed the FOMC speakers on the tape. Rule 7's
third channel — for pricing claims, tone without guidance — was named in the
23 Aug playbook as a general principle and not applied to the one claim
family where it bites hardest. Promoted as its own heuristic (#19).

Secondary: the top of the distribution is still right and still capped —
five structural negatives written at a forced 0.7 against a 22/22 record.
todo-007 remains the fix.

## Playbook changes

Rewritten in full; 18 → 20 heuristics (one merged, three new, eleven
evidence lines extended). Nothing pruned for disuse: every heuristic has an
evidence line inside the last four weeks after this rewrite.

- **#2 merged and extended** (was #2 + #5): "write the flip condition,
  honor it, retire a realized call" now also requires kinetic triggers to
  name the **actor and target class** — evidence: the 5 Sep scan could not
  tell whether a Mehr-reported US strike on an Iranian tanker at Kharg
  anchorage was the stance's "hull strike" trigger; the 6 Sep brief had to
  rewrite the flip line.
- **#3 evidence extended** (price-setter ≠ direction): `031e3470` repeated
  the misread on 2 Sep.
- **#5 revised** (range caps): a cap must survive nothing scheduled inside
  its window that can flip the controlling impulse, and in this trend
  regime a *gold* cap is not written above 0.5 until one hits — evidence:
  gold caps 0/5 since 5 Aug (`f7d39052` `394dfea7` `a2e9d1a8` `7e803666`
  `031e3470`); gold floors 4/4. Open `a3ad30b1` (cap 4554, 0.6) is flagged
  for calibration, not rewritten.
- **#6 revised** (third channel): the third channel is named per claim
  family — tone-without-guidance for pricing, the *other* central bank for
  FX pillars — evidence: `98bbfd33`, `b6450c7a`.
- **#7 revised** (announcement timing ≤0.5): extended to claims about how
  an actor will publicly *frame* an action — evidence: `8babc60f` miss at
  0.6; `55412a56` miss at a compliant 0.5, 0/4 lifetime.
- **#9 evidence extended** (trigger vs target): `9b2f3adf`, `2ef33025`
  both right-reason hits.
- **#10 evidence extended** (structural-negative floor 0.8): 22/22.
- **#11 revised** (pattern extrapolation): a claim with horizon >14 days
  re-verifies its mechanism at the midpoint and is killed if the mechanism
  is dead — evidence: `951d8286`.
- **#12 revised** (operator confirmation): adversary claims are never a
  trigger; incident-count claims score on UKMTO/operator/host-state
  confirmation only — evidence: `c2eafbf8` wrote "OR IRGC-claimed" into its
  trigger and the 2 Sep IRGC claim (CENTCOM-denied sibling, no UKMTO relay)
  satisfies it; a claim that cannot miss cannot inform. Scores on its
  wording 7 Sep, discounted in calibration.
- **#13 evidence extended** (status from the check): the "bars empty" and
  "calendar ends 4 Sep" phantoms this run — see Verified-this-run.
- **#14 revised** (data-event runs): odds leg at print+60; a run that has
  written state and finds inputs missing commits a partial note or
  reschedules — an `empty` exit after state writes is the worst of both —
  evidence: 4 Sep NFP deepdive attempt 1 (todo-180).
- **#15 evidence extended** (dateline/extract freshness): check the
  extracted title against the headline asked for — evidence: 6 Sep Gulf
  News mis-serve (todo-181).
- **#16 evidence extended** (scheduled-date anchors): #41 carried as
  UNVERIFIED, #51/#26 re-verified against feed rows this run.
- **#18 NEW** (horizon alignment): size `horizon_days` so maturity lands at
  or after the claim-text window close; any "claims to score" list derives
  from claim-text windows, never the `matured` flag; no-event claims score
  only at window close — evidence: `8babc60f` flagged ~21h early (28 Aug);
  4 of 8 "matured" claims on 31 Aug had open windows and the wakeup text
  said "score ALL 8"; `b6794dbb`/`fe35b77d` correctly deferred. todo-182.
- **#19 NEW** (pricing thresholds across speaker windows): an odds-level or
  odds-direction claim whose window contains a governor/chair appearance
  is capped at 0.55 unless the speaker's known lean is named as consistent
  with the claim; a "no catalyst in window" premise lists scheduled Fed
  speakers beside data — evidence: 0/3 this fortnight, broken by tone in
  both directions.
- **#20 NEW** (falsifier distance): a level-hold line sits ≥ ~1.5× the
  window's expected range (daily ATR × √days) from spot or is not written
  above 0.55; FX-pillar claims enumerate both central banks' channels —
  evidence: `b6450c7a`; open `a1fc647f` flagged.

## Weight fits (§4.6)

`state/weights.json` exists for the first time — fitted 5 Sep 23:37Z, the
first successful `jamasp weights fit` on this host (PRs #28 and #30 landed
the Yahoo dead-leg fix and the XAUT fallback; todo-012 tracks the unit's
remaining abort-chain and quiet-half items).

**Theme fit** (n=233, 24h horizon, ridge α=1): flags `negative:rates_dollar`,
`negative:supply_mining`, `negative:other`, `negative:etf_flows`.

| Column | β | se | n | fitted |
|---|---|---|---|---|
| rates_dollar | −0.011 | 0.025 | 211 | yes |
| geopolitics | +0.021 | 0.026 | 202 | yes |
| supply_mining | −0.016 | 0.024 | 67 | yes |
| other | −0.002 | 0.024 | 146 | yes |
| physical_cb | +0.003 | 0.024 | 16 | no |
| etf_flows | −0.005 | 0.024 | 3 | no |

Every theme β, negative and positive, is within one standard error of zero.
**No direction-scoring finding survives** — the flags are the sign of noise,
not evidence that bullish-scored rates or mining items were followed by gold
falling. Two days of fits on 233 observations is not a regression to act on;
read again next Sunday. Two structural notes for the record: `physical_cb`
(n=16) and `etf_flows` (n=3) are starved — todo-004's two-vocabulary problem
is the likely reason; and the theme fit's flag list also carries sixteen
technical columns, which the retro skill says to ignore, and which I have.

**Pins:** `config/weights.yaml` `pins: []` — nothing to renew or lapse.

## Human feedback

None forwarded into the repo this fortnight — `feedback:` grep across
`reports/` 24 Aug–6 Sep and `state/lessons-inbox.md` returns nothing, and no
commit since 23 Aug carries feedback. The last items (Saman, 17 Aug) were
both adopted per the 23 Aug retro; todo-008 (Iranian-press + incident
feeds) is open and unchanged.

## Verified-this-run (rule 13 discipline)

- **`jamasp calendar` — alive**, 15 events through 12 Sep: ECB decision
  Thu 10 Sep 12:15Z + presser 12:45Z, PPI/claims 12:30Z, **CPI Fri 11 Sep
  12:30Z**, UoM 14:00Z. Wakeups #51 (13:45Z) and #26 (13:15Z) sit at
  presser+60 / print+45 against feed rows. The stance's "feed ends 4 Sep"
  was Saturday's truth; the thisweek window rolled over Sunday. Corrected
  in stance. todo-001 is the real item — this is the third retro to make
  this same correction.
- **`bars` / `signal_states` / `weight_fits` are NOT empty:** 11,742 bars
  (GC 1d 2020→4 Sep, 1h and 4h through 4 Sep 23:00Z, 1w through 31 Aug),
  48 signal states, 88 fit rows. The stance's "`bars` empty (todo-009)" and
  the 4 Sep `b6450c7a` scoring note ("daily bars table empty") were carried
  from the 26 Aug finding. todo-009 closed with a pointer to 012; stance
  corrected.
- **agent_runs 23 Aug–6 Sep:** 29 failed scans, 3 failed briefs, 4 failed
  deepdives, 1 failed retro — all inside 28–31 Aug (OAuth lapse,
  todo-010/007) and 1 Sep 13–15Z (todo-179) — plus one `empty` on 4 Sep
  (todo-180). Nothing after 4 Sep. No new ops todo.
- **`fe35b77d` DB sweep:** clean (above).
- **Maturity arithmetic** read from `jamasp/predictions.py`: matured ⇔
  `created_at + horizon_days ≤ now`; no window-close field exists. This is
  the root of both 28/31 Aug lessons. todo-182.

## Todos filed / updated this run

- **182 (new):** predictions carry no claim-text window close; `matured`
  fires at creation clock-time + N days and the wakeup/stance text inherits
  the miscount.
- **009 (closed):** the empty-tables problem statement is no longer true;
  residual items (429 on the 730d pull, abort chain, quiet-half watchdog)
  live in 012.
- **012 (appended):** the first successful fit is on record (5 Sep 23:37Z,
  n=233).
- 001, 004, 007, 008, 013, 180, 181: referenced; nothing new to append.

# Jamasp Retro — week ending 2026-09-20

*This retro covers two weeks. The 13 Sep retro failed — `agent_runs` id 453,
exit 1 after 48s, last output "You've hit your session limit · resets 4:30pm
(UTC)". The failure alert delivered (`notify_log` 205/206, ok=1); no brief
from 14–19 Sep mentions it and nothing made it up. That is todo-013's second
occurrence, with a different cause from the first (30 Aug, OAuth). Playbook
last rewritten 6 Sep; the lessons inbox held 24 entries dated 7–18 Sep.*

*59 predictions scored 7–20 Sep: **48 hit, 9 miss, 2 unclear** — 84.2% on
n=57 (6 Sep retro: 65.2% on n=23; 23 Aug: 78.6%). Lifetime 78.4% on n=139
against a stated average of 0.649. Nothing was due this run. Open book: 14 —
three close tonight (`ac946837` 22:00Z; `a0429ae4`, `e0e5cd9c` 23:59Z, both
on hit trajectory) and belong to the Monday brief (rule 18).*

## Scorecard

**By claim type:**

| Type | Record | Avg stated | Read |
|---|---|---|---|
| Structural-negative (no strike on class X / no co-confirmed text / regime persists) | **28/28** + 1 unclear | 0.68 | `fe35b77d` `92a5bbb8` `30bad8a0` `b13614a1` `f527c209` `a12de81f` `973a3143` `75160fe7` `71bd8ddb` `2b39238a` `03627b6b` `302708ab` `2d098d6c` `1f83782a` `2fbc22dd` `110d5daa` `e1e9ba8f` `a8cf8123` `24f8b482` `81e84f0c` `52db03df` `e87144df` `47d2085d` `87e409a7` `dea6f346` `010cb839` `ffd190c0` and `f04926f1` (wording-only: the Yanbu node held while the East-West chain was cut inland). `6b3ad492` UNCLEAR — UKMTO printed "subject to disabling fire", neither a hit nor a position. Family **50/50 lifetime** (3 Aug–19 Sep). **14 of the 28 written at the 0.7 ceiling; 14/14.** |
| Affirmative kinetic ("at least one more X of class C") | **2/4 paper, 0/4 substance** | 0.61 | Misses `81564330` (Iranian fire at GCC bases persists — Iran rotated to warships, then hulls), `c4ef5615` (US strike on Iranian territory — US stayed tanker-for-tanker). "Hits" `c2eafbf8` `bd825267` via a CENTCOM-denied IRGC claim their own trigger text admitted (rule 12 defect, discounted). |
| Fed pricing / decision | **3/5** | 0.63 | Hits `a2171cad` (≥50% at 10 Sep close), `e694f252` (not below 70% into the meeting), `8af1d9b9` (25bp hike) — all with the impulse, inside the blackout. Misses `41aec282` (no ≥60% — 50→70 on NFP/PPI and nine up-days in crude, no speaker), `ec71ce01` (no hike, 0.8, written 7 Aug on one payroll print, 41-day horizon, never midpoint-checked). |
| Gold floors / holds | **4/4** + 1 unclear | 0.59 | `746afbe2` `cdb1709a` `66db8e8e` `63bf960c` — all hit on distance, none showed the named buyer at the line on an oil-UP day; `66db8e8e` held by 4 pts. `a1fc647f` UNCLEAR (GC dark on Labor Day, line inside the proxy basis error). Floors 8/8 this regime. |
| Gold caps | **2/3 paper, 1/3 substance** | 0.53 | `a3ad30b1` HIT right-reason — the first gold cap to hit (blackout removed the speaker channel, PPI/CPI skewed with it, impulse against the level). `f8b7b030` MISS scored at 19:00Z on a 4402.9 running value that finalised at 4382.7 (below the line — wording-ambiguous, todo-187). `b06eaec0` hit by 1.6 pts with no kinetic input: zero information. Gold caps 1/8 on substance since 5 Aug. |
| Gold gap / direction | **3/4** | 0.60 | `58b7daab` (reopen band ±1.5%), `52e1b990`, `54b6127f` (settle lower, price-setter named) hit; `85f08a28` (kinetic gap-up ≥4500) missed — fourth kinetic weekend with no gap. |
| Non-gold levels | **6/8** | 0.58 | Yields 2/2 (`542f76be` by 0.6bp with the wrap below the line; `78317918`), FX 2/2 (`5c6b2df3` `6ddbe426`), oil 1/2 (`b3adcf1e` hit; `65cd057e` Brent <100 missed on an enforcement change with ~1% of room), dollar 1/2 (`a06ae5c5` hit; `09cb81a7` DXY <100 missed on the FOMC presser it never listed). Both misses are against-trend caps. |
| Scheduled event happens | **0/1** | 0.60 | `6c6ed87f` (Oman meeting 14 Sep) — anchored on a participant's announcement; the host postponed. Family 0/7 lifetime. |

**The fortnight in one line:** every claim written *with* the controlling
impulse — structural negatives, floors at ≥1.5× range, with-trend holds,
with-impulse odds thresholds, direction calls that named the price-setter —
hit (**44/44**, avg stated 0.64); every claim written *against* it — caps on
gold, Brent, DXY and Fed odds, "one more strike of class C", a kinetic
gap-up, a scheduled meeting, a 41-day no-hike — went **4/13 on paper, 1/13 on
substance**.

**Direction split:** flat 42/47; **up 3/6 on paper, 0/6 on substance** (two
wording-only kinetic hits, one 0.6bp yield hit the wrap contradicted); down
3/4.

**Where I'm reliably right:** structural negatives (50/50, still
ceiling-capped); floors sized by rule 20; with-impulse threshold claims when
no speaker sits in the window; "down" calls that name the chain (oil → 2y →
DXY) rather than a haven.

**Where I overweight noise:**

1. **Affirmative kinetic claims on a single target class.** 0/4 on
   substance. Iran's next act after a US counter landed on a different class
   every cycle (bases → warships → hulls → corridor); the US did not rotate
   off hulls for fifteen days despite the rhetoric. I was writing "the last
   act repeats" in both directions.
2. **Against-impulse caps.** 1/6 on substance across gold, Brent, DXY and
   odds. `65cd057e` called the Brent premium "insurance-priced" — which is a
   reason a cap is fragile to an enforcement change, not a reason to write
   it. `09cb81a7` had an FOMC presser inside its window and listed only CPI.
3. **Wording that cannot miss.** Four claims this fortnight carried no
   information about the mechanism they claimed to test: `c2eafbf8`,
   `bd825267` ("or IRGC-claimed"), `b06eaec0` (a plain price cap as a
   non-transmission test), `f04926f1` (node named, chain argued).
4. **The one high-confidence miss.** `ec71ce01` at 0.8 cost more calibration
   than the other eight misses together — a 41-day claim on one data point,
   with rule 11's midpoint check falling on Warsh's Jackson Hole speech and
   never run.

## Calibration notes

Stated avg **0.632** vs observed **0.842** on n=57 — the widest
underconfidence on record (6 Sep: 0.685 vs 0.652 overconfident; before that
three underconfident weeks). The bucket view:

| Stated | Record | Observed |
|---|---|---|
| 0.50 | 1/2 | 50% |
| 0.55 | 12/14 | 86% |
| 0.60 | 11/15 | 73% |
| 0.65 | 6/7 | 86% |
| 0.70 | 14/14 | **100%** |
| 0.75 | 1/1 | 100% |
| 0.80 | 3/4 | 75% |

Monotone breaks twice: 0.60 sits below 0.55, and 0.80 below 0.70. The 0.70
row is the brief ceiling (todo-007) sitting on fourteen structural
negatives; re-pricing those at the rule-10 floor lifts the stated average to
~0.66 and explains a third of the gap. The rest is the 0.55 row: twelve hits,
mostly floors and holds written at the rule-20 marginal — with the impulse,
≥1.5× range from the line — and priced as coin-flips-plus.

**The concrete bias: I price the falsifier's distance, not the impulse's
direction.** Claims at 0.55–0.60 *with* the controlling impulse hit 20/21;
claims at the same labels *against* it hit 3/8 on paper, 1/8 on substance.
The confidence label tracked how far the line was; it should track which way
the desk with the wheel is pushing. Correction written into rules 5 and 20:
a with-impulse hold at ≥1.5× range starts at 0.65; an against-impulse cap
tops at 0.5 unless the three conditions rule 5 now names all hold.

Secondary: the 0.60 bucket is still the hedge label — 3 of its 4 misses are
against-impulse or affirmative-kinetic shapes that rule 5, 19 and 21 now cap
at 0.5, and the fourth is the announcement family (rule 7).

## Playbook changes

Rewritten in full; 20 → 23 heuristics (three new, three merged-and-revised,
fourteen evidence lines extended). Nothing pruned for disuse: every rule has
evidence inside the last four weeks after this rewrite.

- **#5 revised** (caps): a gold cap may reach 0.6 only when the controlling
  desk's impulse is against the level AND no Fed speaker sits in the window
  AND the in-window data consensus skews with the cap; any cap inside one
  expected range after an event day, or inside an active enforcement
  campaign with <1 ATR of room, tops at 0.5; a "premium, not barrels" cap
  names the enforcement change as its first breaker — evidence: `a3ad30b1`
  hit under all three conditions; `f8b7b030` (65 pts on ATR 110 after a
  −1.9% day, impulse flipped session one), `65cd057e` (~1% room, IRGC
  turn-backs → disabling fire), `09cb81a7` (FOMC inside the window) missed.
  Gold caps 1/8 on substance since 5 Aug; non-gold flow caps 2/4; every
  floor/hold this fortnight 12/12.
- **#6 evidence extended** (third channel): tone without guidance now 4/4
  for ≥10-pt Fed repricings (16 Sep: no guidance sentence, presser moved Oct
  odds 38 → 55–58); the oil leg is the third channel for every "hot print →
  gold down" line (11 Sep: core 0.318 fired the flip, gold +1.4% on the
  Hormuz-plan oil drop).
- **#7 revised** (announcement family): extended to "scheduled event
  happens" claims anchored on a participant's announcement — evidence:
  `6c6ed87f` (Baghaei-only anchor, host postponed); 0/7 lifetime.
- **#8 merged and revised** (no-incident claims): the falsifier states
  whether "fired upon" counts and names the source that resolves position,
  or drops the qualifier; an infrastructure claim names the whole chain it
  argues about or states node scope explicitly — evidence: `6b3ad492`
  UNCLEAR, `f04926f1` wording-only; `81e84f0c` `47d2085d` scoped correctly
  and hit.
- **#9 evidence extended** (trigger vs target): four floors hit on distance
  with the named buyer unobserved; `cdb1709a` the first whose breaker fired
  and failed.
- **#10 evidence extended**: 50/50; 14/14 at the ceiling this fortnight.
- **#11 evidence extended** (midpoint check): `ec71ce01`.
- **#12 evidence extended** (adversary claims): `c2eafbf8` `bd825267` scored
  hit on a CENTCOM-denied claim — two hits carrying zero information.
- **#13 revised** (status from the check): applies to trajectory calls
  (sweep the whole window from `created_at`, cite the earliest qualifying
  event) and to feed-gap claims — evidence: 6 Sep "miss trajectory" on
  `bd825267`/`c2eafbf8` from the latest event only; the 11 Sep deepdive's
  "no ingested item carried the Hormuz-plan report" was false — the
  Investing.com commodities feed carried it at 01:01Z, twelve hours earlier
  (verified this run).
- **#14 revised** (data-event runs): schedule off the US print, never the
  foreign presser; read the investinglive print post for the odds sentence
  first, actionforex's front at print+50 second, and declare "no relay"
  only after both; a hot/soft verdict names the largest single component
  and the ex-that number before the odds sentence — evidence: deepdive #53
  (ECB moved GC 2 pts, PPI 30; odds in the print post at print+15), #26
  (print+45 worked; wireless +5.9% was 0.1pp of core).
- **#15 evidence extended**: 14 Sep Gulf News "wounded USAF officer" was an
  April shoot-down interview, not a new event.
- **#16 revised** (date anchors): a diplomatic-event anchor cites the host
  or venue state; participant-only announcements schedule a scan marker,
  not a deepdive — evidence: wakeup #55 on Baghaei's Mehr line; Oman's
  postponement reached the desk 19:37Z Sunday, before the reopen.
- **#17 revised and merged** (one basis): a level claim names ONE basis —
  COMEX settle via relay OR the GC_CLOSE row read next morning; `^TNX` row
  OR wrap close; proxy series plus basis — sits outside the two readings'
  disagreement (~25 pts GC, ~5bp 10y, one basis-width for a proxy), and is
  never scored on an intraday value; a scan seeing a breach on the other
  basis marks "breach trajectory" and leaves the score to the brief —
  evidence: `a1fc647f` UNCLEAR, `542f76be` by 0.6bp with the wrap below,
  `f8b7b030` scored at 19:00Z on a value that finalised 20 pts lower,
  `b06eaec0` by 1.6 pts. todo-187.
- **#18 evidence extended**: 12 Sep brief listed `110d5daa`/`e1e9ba8f` for
  the wrong day; `a06ae5c5` scored from window close before the horizon
  tick, correctly. todo-182.
- **#19 revised** (threshold claims across an appearance): a USD/yield
  LEVEL claim spanning an FOMC decision, presser or CPI/NFP print lists it
  as a breaker and caps at 0.5 unless the line sits ≥1.5× range from spot
  including the event day's typical move; while an oil shock is live
  (Brent up ≥5 straight sessions, or a UKMTO-confirmed enforcement change
  inside the window) a Fed-pricing threshold with <15 pts of room over >3
  sessions caps at 0.5; a bracket pair is written as one band claim —
  evidence: `41aec282` (50 → 70 with no speaker), `09cb81a7` (1.3× range,
  FOMC inside); `a2171cad` `e694f252` `78317918` with the impulse, hit.
- **#20 revised** (falsifier distance): a with-impulse hold at ≥1.5× range
  starts at 0.65 — evidence: floors 8/8 this regime at an average 0.58;
  `66db8e8e` held by 4 pts on an oil-UP session (the marginal that nearly
  broke).
- **#21 NEW** (target class): "at least one more X of class C inside ≤7
  days", or "actor changes class", caps at 0.5 unless a stated policy
  trigger or scheduled gate for that class is named; the default is the
  structural negative on the current class or the class-agnostic version —
  evidence: `81564330` `c4ef5615` misses; `c2eafbf8` `bd825267`
  wording-only; hull-class negatives `03627b6b` `e1e9ba8f` `87e409a7` 3/3.
- **#22 NEW** (the regime chain): oil → 2y → DXY → gold; an escalation
  reaches gold as a hike and a margin call, not a haven bid; a kinetic
  gap-up or "≥X in session one" claim is not written above 0.5 while hike
  odds are ≥50% (write the band); a yen or foreign-CB leg for gold states
  the DXY move it requires (≥0.3 or a cross of 100) or is subordinate to
  the oil/long-end channel; a named risk that has printed ≥3 times without
  moving gold against the claim leaves that family's risk list — evidence:
  `85f08a28` miss / `58b7daab` hit (fourth kinetic weekend with no gap;
  `ac946837` tonight is the fifth); `52e1b990` `54b6127f` hits with the
  chain named; twelve non-transmission sessions 2–18 Sep; BoJ 18 Sep
  (USDJPY +0.6%, DXY +0.07, gold +1.2% on Brent −2.5%).
- **#23 NEW** (tape clause): every re-weight flip — kinetic, rates-data,
  Fed-event — carries the named trigger AND GC in the flip's direction from
  the trigger's first print to that session's Globex close (the next
  settle if the trigger prints after 17:30Z); a Fed-event flip is written on
  the next-meeting odds sentence at the wrap plus print-to-Globex-close,
  never on statement wording; a flip that fires on the letter but fails on
  the sequence moves the weight at most 5 pts — evidence: 9 Sep (second
  CENTCOM tanker strike printed, gold −1.5%); 11 Sep CPI (numbers fired,
  gold up); 11 Sep East-West pipeline (daily +1.14% set before the
  confirmation, −20 pts after it); 16 Sep FOMC (no guidance sentence, the
  presser delivered the hard branch; the 17:30Z settle preceded the 18:00Z
  trigger).

## Weight fits (§4.6)

`state/weights.json` fitted 19 Sep 23:30Z. **Theme fit** n=427 (6 Sep:
233), 24h horizon, ridge α=1. Flags: `negative:supply_mining` only.

| Column | β | se | n | fitted | multiplier |
|---|---|---|---|---|---|
| geopolitics | **+0.050** | 0.019 | 388 | yes | 1.89 |
| rates_dollar | +0.034 | 0.018 | 394 | yes | 1.28 |
| other | +0.015 | 0.018 | 273 | yes | 0.57 |
| physical_cb | +0.004 | 0.018 | 31 | no | 1.00 |
| etf_flows | +0.029 | 0.018 | 7 | no | 1.00 |
| supply_mining | −0.007 | 0.019 | 124 | yes | 0.25 |

The flagged column is 0.4 se from zero — noise, same as the four flags on
6 Sep; no direction-scoring change on mining. The finding is elsewhere:
**geopolitics is the first theme coefficient beyond two standard errors
(2.6σ), positive** — items scored bullish on geopolitics were followed by
gold *up* over 24h across the whole sample. My September read (rule 22:
escalation reaches gold as a hike, twelve non-transmission sessions) says the
opposite for the last three weeks. The two reconcile only if the regime is
shorter than the fit's sample or the September items are a minority of
n=388; the fit cannot be windowed to test that (a `--since` on `weights
fit` would be a code change, not filed — it is a curiosity until the sign
persists). **No pin**: the retro overrides a fitted value on evidence, and
the evidence here is a disagreement, not a defect. Re-read next Sunday; if
geopolitics stays >2σ positive while rule 22's tape holds, the direction
scorer, not the theme, is the suspect. `physical_cb` (31) and `etf_flows`
(7) remain starved — todo-004.

**Technical fit:** 24 `negative:` flags of 38 columns — expected noise per
the skill, ignored.

**Pins:** `config/weights.yaml` `pins: []` — nothing to renew or lapse.

## Human feedback

None forwarded into the repo this fortnight — `feedback:` grep across
`reports/2026/09/` and `state/lessons-inbox.md` returns only the 6 Sep
retro's own "none" line; no commit since 6 Sep carries a feedback entry.
SamMHD's PRs #38 (13 Sep, panel i18n + `jamasp translate`) and #39 (20 Sep,
strict output schema) are code contributions, not analysis feedback —
nothing to adopt or decline. Todo-008 (Saman's 17 Aug source approvals)
is open and unchanged.

## Verified-this-run (rule 13 discipline)

- **`jamasp calendar` — alive**, 21 events to 26 Sep (Lagarde Mon/Tue, SNB
  Thu 07:30Z, US claims Thu, revised UoM Fri). The stance's "calendar feed
  ended 19 Sep" was again Saturday's truth carried past the Sunday
  rollover — todo-001's shape, no new item. **The stance's "no Fed-speaker
  schedule in any feed" is false:** the `events` table holds 33 "FOMC
  Member … Speaks" rows tagged Low — Goolsbee Mon 21 Sep 10:30Z; Williams
  08:10Z, Hammack 12:50Z, Paulson 14:10Z Thu 24 Sep — and `jamasp calendar`
  filters to High/Medium. The Fed's own calendar page does not extract.
  Stance and watchlist corrected this run; todo-188. Three open claims
  (`aec5cc8b` `1889819a` `be4d6b22`) span those speakers and list none as
  a breaker — the Monday brief's rule-19 job.
- **`jamasp price`** — GC 4415.9 (18 Sep last tick), GC_CLOSE 4424.9,
  2y/10y/DXY Friday closes as the stance states; ATR14 109, 50DMA 4326.5,
  200DMA 4554.3. Consistent with the 20 Sep brief.
- **agent_runs 6–20 Sep:** 117 ok scans, 15 briefs, 6 deepdives; one
  failed scan (6 Sep 17:00Z) and the failed retro (13 Sep). The retro
  failure's alert delivered to the desk chat in both languages
  (`notify_log` 205/206); the make-up never happened — todo-013.
- **The 11 Sep "feed gap" was a phantom.** `items` carries "Oil prices
  reverse course, fall 1% on report of Iran, Oman seeking Hormuz deal"
  (investing_commodities, 11 Sep 01:01Z) — the report the CPI deepdive
  said no feed carried. Wire-class headlines 7–20 Sep: forexlive 17,
  investing_commodities 10, mining_weekly 4, gcaptain 3, gulf_news 2. **No
  wire-relay source todo** — the gap was not running the keyword query
  (rule 13) plus the scan-consumed delta (todo-184).
- **`predictions due`** — 0 matured unscored; the three closing tonight are
  correctly not listed (horizon lands after window close).

## Todos filed / updated this run

- **187 (new):** `jamasp price` GC_CLOSE is the Globex running value, not
  the COMEX settle, and is not labelled provisional — `f8b7b030` scored on
  it.
- **188 (new):** `jamasp calendar` hides every non-chair FOMC speaker
  (ForexFactory tags them Low); rule 19 cannot be applied from the tool
  while the feed already has the rows.
- **013 (appended):** second failed retro (13 Sep, session limit at
  16:00Z, resets 16:30Z); alert delivered, unnoticed for a week; the
  make-up question in §2 and the timer slot.
- **182 (appended):** 13 Sep recurrence.
- **007 (appended):** 14/14 at the 0.7 ceiling; family 50/50.
- **183 (appended):** `a1fc647f` UNCLEAR on the dark feed.
- 001, 004, 180, 184: referenced; nothing new to append.
